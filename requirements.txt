# Runtime dependencies. tkinter is part of the standard library on Windows.
hidapi>=0.14
pillow>=10.0
psutil>=5.9
windnd>=1.0.7          # Windows file drop onto the preview canvas
tzdata>=2024.1         # IANA zones for world clocks (zoneinfo needs this on Windows)
